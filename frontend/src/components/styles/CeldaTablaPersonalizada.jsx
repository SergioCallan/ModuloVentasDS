import { TableCell} from "@mui/material";
import styled from "@emotion/styled";

export const TCelda = styled(TableCell)(() => ({
    textAlign: 'center'
  }));

export const TCelda2 = styled(TableCell)(() => ({
    textAlign: 'center',
    padding: 0,
    margin: 0,
}));